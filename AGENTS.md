# Output formatting

- Don't use em dash (—), also known as m-dash or mdash.
- Don't use en dash (–), also known as n-dash or ndash.
- When a dash is needed, always use a minus (-) sign instead.
- If you encounter mdash or ndash in existing text, silently replace it with a minus sign (-) without asking for confirmation.
